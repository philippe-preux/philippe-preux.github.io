dans slides/ il y a un seul fichier : index

Sur le serveur grappa, dans ce répertoire :
index est la cible de 214 liens symboliques obtenus par 
    for i in `seq 0 213` ; do ln -s index index-$i.php; done

les ecl.big.data-*.png ont été copiés.
Ils ont été obtenus par 
    convert ecl.big.data.pdf ecl.big.data.png


On peut aussi mettre des notes associées à chaque slide dans un fichier 
   notes-xxx.html
Il est chargé automatique après la diapo.

